## Testrepo

This repo contains a lot of crap (tags, branches, etc). It's main purpose is for testing some CI/CD stuff. While the image is running and pretty usefull (at least `:latest`), everything else should not be used.